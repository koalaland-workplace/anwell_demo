# Tài liệu nội bộ — KHÔNG đưa lên repo public.
# Chứa hồ sơ mời đầu tư, định giá, dự toán, phân tích rủi ro.
Tailieu-noibo/

# macOS
.DS_Store

# Cấu hình Claude Code riêng từng máy — chứa đường dẫn cá nhân và tên tệp nội bộ.
# Thư mục .claude/skills/ thì VẪN commit: đó là quy tắc chung của dự án.
.claude/settings.local.json

# Nhật ký chi phí token — do T7/T8 tự ghi mỗi lượt chạy.
# Không commit: T8 là vai CHỈ ĐỌC, ghi vào cây làm việc thì lần rà sau lại thấy
# chính dòng nhật ký của lần trước trong diff.
tools/moi/nhat-ky-chi-phi.tsv

# Bộ nhớ kết quả rà — tránh chạy lại trên nội dung không đổi.
tools/moi/.da-ra/

# Số liệu vận hành đội AI — hạn mức tài khoản và chi phí token.
# Trang admin đọc tệp này khi chạy cục bộ; bản công khai không có nên để trống.
assets/doi-ai.js

# Bảng theo dõi đội AI — chứa hạn mức và chi phí token.
# Sinh lại bằng: python3 tools/dashboard-doi.py
docs/dashboard-doi.html
